/* import 'package:intl/intl.dart';

DateTime now = new DateTime.now();
late DateTime date = new DateTime(now.year, now.month, now.day);
late DateTime data;
late DateTime dateCompare;
dataSet() {
  dateCompare = ;
  data = date;
  print(date);
  print(dateCompare);
  return date;
}

dataUpdate() {
  if (data == dateCompare) {
    dataSet();
  }
} */
